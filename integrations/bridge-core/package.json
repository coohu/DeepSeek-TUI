{
  "name": "@deepseek/bridge-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared pure helpers for DeepSeek chat bridges.",
  "main": "src/lib.mjs",
  "scripts": {
    "check": "node --check src/lib.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
